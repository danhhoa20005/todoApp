<!-- // Nền gradient cho thanh tiến độ tổng số công việc hoàn thành -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bo góc -->
    <corners android:radius="6dp" />

    <!-- Viền ngoài -->
    <stroke
        android:width="2dp"
        android:color="@color/text_gray" /> <!-- tím trung tính -->

    <!-- Màu nền trong -->
    <solid android:color="@color/done_color" /> <!-- tím than nhẹ -->
</shape>
